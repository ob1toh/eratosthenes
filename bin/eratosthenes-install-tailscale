#!/bin/bash

# eratosthenes:summary=Install the Tailscale mesh VPN service and a web app for the Tailscale Admin Console.
# eratosthenes:requires-sudo=true

echo -e "\nInstalling Tailscale..."
eratosthenes-pkg-add tailscale

echo -e "\nStarting Tailscale..."
sudo systemctl enable --now tailscaled.service
sudo tailscale up --accept-routes

eratosthenes-webapp-install "Tailscale" "https://login.tailscale.com/admin/machines" https://cdn.jsdelivr.net/gh/homarr-labs/dashboard-icons/png/tailscale-light.png
