#!/bin/bash

# eratosthenes:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/eratosthenes/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! eratosthenes-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
