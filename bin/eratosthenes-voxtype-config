#!/bin/bash

# eratosthenes:summary=Open the Voxtype configuration file

set -e

# Used by Voxtype waybar module to open config on right click

exec eratosthenes-launch-editor ~/.config/voxtype/config.toml
