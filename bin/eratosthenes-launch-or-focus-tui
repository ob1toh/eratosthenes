#!/bin/bash

# eratosthenes:summary=Launch a TUI or focus an existing terminal window for it
# eratosthenes:args=<command> [args...]

APP_ID="org.eratosthenes.$(basename "$1")"
LAUNCH_COMMAND="eratosthenes-launch-tui $@"

exec eratosthenes-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
