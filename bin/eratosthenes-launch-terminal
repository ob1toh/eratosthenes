#!/bin/bash

# eratosthenes:summary=Launch a terminal in the active terminal's current directory
# eratosthenes:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(eratosthenes-cmd-terminal-cwd)" "$@"
