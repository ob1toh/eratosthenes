#!/bin/bash

# eratosthenes:summary=Overwrite the user configs for the Walker application launcher (which also powers the Eratosthenes Menu) and restart the services.

mkdir -p ~/.config/autostart/
cp $ERATOSTHENES_PATH/default/walker/walker.desktop ~/.config/autostart/

# And restarts if it crashes or is killed
mkdir -p ~/.config/systemd/user/app-walker@autostart.service.d/
cp $ERATOSTHENES_PATH/default/walker/restart.conf ~/.config/systemd/user/app-walker@autostart.service.d/restart.conf

systemctl --user daemon-reload

# Refresh configs
eratosthenes-refresh-config walker/config.toml
eratosthenes-refresh-config elephant/calc.toml
eratosthenes-refresh-config elephant/desktopapplications.toml

# Link all elephant menus
mkdir -p ~/.config/elephant/menus
for menu in $ERATOSTHENES_PATH/default/elephant/*.lua; do
  ln -snf "$menu" ~/.config/elephant/menus/"$(basename "$menu")"
done

# Restart service
eratosthenes-restart-walker
