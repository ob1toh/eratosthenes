#!/bin/bash

# eratosthenes:summary=Remove a supported browser and clean up Eratosthenes browser defaults
# eratosthenes:args=<chrome|brave|brave-origin|edge|firefox|zen>
# eratosthenes:examples=eratosthenes remove browser firefox | eratosthenes remove browser brave
# eratosthenes:requires-sudo=true

set_fallback_default_browser() {
  local current_browser
  current_browser=$(xdg-settings get default-web-browser)

  if [[ $current_browser != "$1" ]]; then
    return
  fi

  if eratosthenes-cmd-present chromium; then
    xdg-settings set default-web-browser chromium.desktop
    xdg-mime default chromium.desktop x-scheme-handler/http
    xdg-mime default chromium.desktop x-scheme-handler/https
    xdg-mime default chromium.desktop text/html
  fi
}

case $1 in
chrome)
  echo "Removing Chrome..."
  set_fallback_default_browser google-chrome.desktop
  eratosthenes-pkg-drop google-chrome
  rm -f ~/.config/chrome-flags.conf
  sudo rm -f /etc/opt/chrome/policies/managed/color.json
  ;;
edge)
  echo "Removing Edge..."
  set_fallback_default_browser microsoft-edge.desktop
  eratosthenes-pkg-drop microsoft-edge-stable-bin
  rm -f ~/.config/microsoft-edge-stable-flags.conf
  sudo rm -f /etc/opt/edge/policies/managed/color.json
  ;;
brave)
  echo "Removing Brave..."
  set_fallback_default_browser brave-browser.desktop
  eratosthenes-pkg-drop brave-bin
  rm -f ~/.config/brave-flags.conf

  if eratosthenes-pkg-missing brave-origin-beta-bin; then
    sudo rm -rf /etc/brave
  fi
  ;;
brave-origin)
  echo "Removing Brave Origin..."
  set_fallback_default_browser brave-origin-beta.desktop
  eratosthenes-pkg-drop brave-origin-beta-bin
  rm -f ~/.config/brave-origin-beta-flags.conf

  if eratosthenes-pkg-missing brave-bin; then
    sudo rm -rf /etc/brave
  fi
  ;;
firefox)
  echo "Removing Firefox..."
  set_fallback_default_browser firefox.desktop
  eratosthenes-pkg-drop firefox
  ;;
zen)
  echo "Removing Zen..."
  set_fallback_default_browser zen.desktop
  eratosthenes-pkg-drop zen-browser-bin
  ;;
*)
  echo "Usage: eratosthenes-remove-browser <chrome|brave|brave-origin|edge|firefox|zen>"
  exit 1
  ;;
esac
