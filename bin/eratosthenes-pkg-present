#!/bin/bash

# eratosthenes:summary=Returns true if all of the named packages are installed on the system (or false if any of them are missing).
# eratosthenes:args=<packages...>

for pkg in "$@"; do
  pacman -Q "$pkg" &>/dev/null || exit 1
done

exit 0
